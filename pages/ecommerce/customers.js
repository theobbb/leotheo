import { Toolbar, Typography } from "@mui/material";
import Layout from "../../components/layout";

export default function Customers() {
    return (
        <Layout>
            <Toolbar><Typography variant="h4">Customers</Typography></Toolbar>
            
            
        </Layout>
    )
}
Customers.auth = true;