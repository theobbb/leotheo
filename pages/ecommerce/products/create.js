import { Toolbar, Typography } from "@mui/material";
import Layout from "@layouts/main";

export default function CreateProduct() {
    return (
        <Layout>
            <Toolbar><Typography variant="h4">Create</Typography></Toolbar>
            
            
        </Layout>
    )
}
CreateProduct.auth = true;
